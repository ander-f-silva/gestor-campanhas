server:
  port: 9080

default:
  brocker:
    url: vm://localhost?broker.persistent=false

queue:
  campanha: campanha-queue

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9082/eureka/